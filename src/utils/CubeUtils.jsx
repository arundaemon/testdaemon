import axios from 'axios'
import EnvSettings from "../config/settings";
import { useCubeQuery } from '@cubejs-client/react';

export const subscribeCubeData = (query) => {
    // let instance = axios.create();
    // let cubeToken = ''
    // let headers = { 'Authorization': localStorage.getItem('cubeToken') }
    // delete instance.defaults.headers.common['accessToken'];
    // return instance.post(`${EnvSettings.CUBE_API_URL}/cubejs-api/v1/load`, payload, { headers })

    // const { resultSet, error, isLoading } = useCubeQuery(query, { subscribe: true });
} 