import settings from "../config/settings";

export const VerifyJwt = (token) => {
    // return jwt.verify(token, settings.AUTH_SECRET, (err, decoded) => {
    //     if (err) {
    //         return { authorised: false }
    //     }
    //     else {
    //         return { authorised: true, tokenData: decoded }
    //     }
    // })

}

